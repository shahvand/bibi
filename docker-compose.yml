services:
  web:
    build: .
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      - db
    networks:
      - bibi_network
    environment:
      - DEBUG=True
      - SECRET_KEY=qwerqwew4c5w45@##545454
      - DB_NAME=bibi
      - DB_USER=bibi_user
      - DB_PASSWORD=coweorw@#k
      - DB_HOST=db
      - DB_PORT=3306
      - LOAD_SAMPLE_DATA=False
      - ALLOWED_HOSTS=localhost,127.0.0.1,93.118.119.250
    volumes:
      - ./static:/app/static
      - ./media:/app/media
      - ./staticfiles:/app/staticfiles

  db:
    image: mysql:8.0
    restart: always
    networks:
      - bibi_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "bibi_user", "-pcoweorw@#k"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 60s
    # حذف انتشار پورت به بیرون - فقط برای استفاده داخلی سرویس web
    # ports:
    #   - "3306:3306"
    environment:
      - MYSQL_DATABASE=bibi
      - MYSQL_USER=bibi_user
      - MYSQL_PASSWORD=coweorw@#k
      - MYSQL_ROOT_PASSWORD=rAdmin12@x
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    cap_add:
      - SYS_NICE  # برای کاهش مشکلات احتمالی عملکرد MySQL

volumes:
  mysql_data:

networks:
  bibi_network:
    driver: bridge 